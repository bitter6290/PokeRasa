public readonly struct LearnsetMove
{
    public readonly int level;
    public readonly MoveID move;
    public LearnsetMove(int Level, MoveID Move)
    {
        level = Level;
        move = Move;
    }
}